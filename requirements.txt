# =====================================================================
# Chatbot project — fully pinned, mutually-compatible dependency set.
# Verified: all langchain/langgraph/mcp packages import together on
# langchain-core 0.3.72 (the 0.3.x generation).
#
# Install into a fresh venv:   pip install -r requirements.txt
#
# IMPORTANT: do NOT `pip install -U` any single langchain-* / langgraph-*
# package on its own — they are tightly coupled and upgrading one breaks
# the others. Change versions here and reinstall the whole file instead.
# =====================================================================

# --- UI ---
streamlit==1.47.1

# --- LangChain / LangGraph core (KEEP THESE PINNED TOGETHER) ---
langchain-core==0.3.72
langchain==0.3.27
langchain-community==0.3.27
langchain-text-splitters==0.3.9
langchain-ollama==0.3.6
langgraph==0.6.1
langgraph-checkpoint==2.1.1
langgraph-checkpoint-sqlite==2.0.11
langgraph-prebuilt==0.6.1
langsmith>=0.1,<0.5          # tracing

# --- MCP (email server + adapters; KEEP mcp on the 1.x line) ---
langchain-mcp-adapters==0.1.14
mcp>=1.9,<2

# --- RAG: vector store + hybrid + rerank + loaders ---
langchain-chroma>=0.2,<0.3     # 0.2.x allows numpy 2 (required on Python 3.13)
chromadb>=0.5
rank-bm25>=0.2
flashrank>=0.2
pypdf>=4
docx2txt>=0.8

# --- YouTube / audio / news tools ---
youtube-transcript-api>=0.6
gTTS>=2.5
feedparser>=6

# --- misc ---
python-dotenv>=1
requests>=2.31
yfinance>=0.2